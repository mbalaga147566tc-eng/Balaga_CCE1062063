import { StyleSheet, Text, View } from 'react-native';
type Props = { title: string; value: string | number; icon?: string; wide?: boolean; width?: number };
export default function StatCard({ title, value, icon, wide, width }: Props) { return <View style={[styles.card, { width: width ?? (wide ? '31%' : '48%') }]}>{icon ? <Text style={styles.icon}>{icon}</Text> : null}<Text style={styles.value}>{value}</Text><Text style={styles.title}>{title}</Text></View>; }
const styles = StyleSheet.create({ card: { backgroundColor: '#fff', borderRadius: 18, padding: 16, minHeight: 128, justifyContent: 'space-between', marginBottom: 12, shadowColor: '#1e3a8a', shadowOpacity: 0.08, shadowRadius: 12, elevation: 2 }, icon: { fontSize: 21 }, value: { fontSize: 28, fontWeight: '800', color: '#172554' }, title: { fontSize: 13, color: '#64748b', fontWeight: '600' } });
