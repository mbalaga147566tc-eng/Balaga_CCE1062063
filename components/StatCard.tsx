import { StyleSheet, Text, View } from 'react-native';
type Props = { title: string; value: string | number; icon?: string; wide?: boolean; width?: number };
export default function StatCard({ title, value, icon, wide, width }: Props) { return <View style={[styles.card, { width: width ?? (wide ? '31%' : '48%') }]}><View style={styles.iconWrap}>{icon ? <Text style={styles.icon}>{icon}</Text> : null}</View><Text style={styles.value}>{value}</Text><Text style={styles.title}>{title}</Text></View>; }
const styles = StyleSheet.create({ card: { backgroundColor: '#FFFFFF', borderRadius: 20, padding: 16, minHeight: 142, justifyContent: 'space-between', marginBottom: 12, borderWidth: 1, borderColor: '#E8EEF2', shadowColor: '#0F172A', shadowOpacity: 0.06, shadowRadius: 12, shadowOffset: { width: 0, height: 5 }, elevation: 2 }, iconWrap: { width: 34, height: 34, borderRadius: 12, backgroundColor: '#E6FFFA', alignItems: 'center', justifyContent: 'center' }, icon: { fontSize: 18 }, value: { fontSize: 30, fontWeight: '800', color: '#0F172A', letterSpacing: -0.8 }, title: { fontSize: 13, color: '#64748B', fontWeight: '700' } });
