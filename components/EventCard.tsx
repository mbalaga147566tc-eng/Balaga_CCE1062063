import { Pressable, StyleSheet, Text, View } from 'react-native';
import { Event } from '../data/events';
type Props = { event: Event; onPress: () => void; isJoined?: boolean };
export default function EventCard({ event, onPress, isJoined }: Props) { const joined = isJoined ?? event.joined; return <Pressable onPress={onPress} style={({ pressed }) => [styles.card, pressed && styles.pressed]}><View style={styles.top}><View style={styles.category}><Text style={styles.categoryText}>{event.category}</Text></View><Text style={[styles.status, joined && styles.joined]}>{joined ? 'Joined' : 'Available'}</Text></View><Text style={styles.title}>{event.title}</Text><Text style={styles.detail}>◷  {event.date} · {event.time}</Text><Text style={styles.detail}>⌖  {event.venue}</Text><Text style={styles.action}>View details  →</Text></Pressable>; }
const styles = StyleSheet.create({ card: { backgroundColor: '#fff', borderRadius: 18, padding: 18, marginBottom: 14, shadowColor: '#1e3a8a', shadowOpacity: 0.07, shadowRadius: 10, elevation: 2 }, pressed: { opacity: 0.8, transform: [{ scale: 0.99 }] }, top: { flexDirection: 'row', justifyContent: 'space-between', marginBottom: 13 }, category: { backgroundColor: '#eff6ff', borderRadius: 20, paddingHorizontal: 10, paddingVertical: 5 }, categoryText: { color: '#2563eb', fontSize: 12, fontWeight: '700' }, status: { color: '#64748b', fontSize: 12, fontWeight: '700' }, joined: { color: '#059669' }, title: { color: '#172554', fontSize: 18, fontWeight: '800', marginBottom: 11 }, detail: { color: '#64748b', fontSize: 13, marginBottom: 6 }, action: { color: '#2563eb', fontSize: 14, fontWeight: '800', marginTop: 10 } });
